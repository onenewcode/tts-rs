# Legacy example retained only to prevent accidental reuse of the old registry
# shape during the refactor. It is no longer the target configuration format.
#
# New target:
# - package-first CLI input
# - per-package manifest
# - no global models registry
#
# See `docs/qwen3_tts_package.example.yaml` and
# `docs/refactor/06-package-manifest-example.md`.
legacy_models_registry_retired: true
